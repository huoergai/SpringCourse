# 此配置文件是为解决实体有继承的情况下，子类使用@Data会有警告提示。
# 解决：1.在java目录下添加此配置文件；2.在各个子类实体中添加：@ToString(callSuper = true)
#                                   @EqualsAndHashCode(callSuper = true)

config.stopBubbling=true
lombok.equalsAndHashCode.callSuper=CALL